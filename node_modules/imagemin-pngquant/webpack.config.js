const path = require('path');
const webpack = require('webpack');
const NodePolyfillPlugin = require('node-polyfill-webpack-plugin');
const NodeLibsBrowserPlugin = require('node-libs-browser-webpack-plugin');

module.exports = {
  entry: './index.js',
  target: 'web',
  resolve: {
    alias: {
      fs: 'node-libs-browser/mock/fs',
      os: 'node-libs-browser/mock/os'
    }
  },
  output: {
    filename: 'imagemin-pngquant.browser.js',
    path: path.resolve(__dirname, 'dist'),
    library: {
      name: 'imageminPngquant',
      type: 'umd'
    }
  },
  plugins: [
    new NodePolyfillPlugin(),
    new NodeLibsBrowserPlugin(), // 添加 node-libs-browser 插件
    new webpack.DefinePlugin({
      'process.env.NODE_ENV': JSON.stringify('production')
    }),
    new webpack.ProvidePlugin({
      process: 'process/browser'
    })
  ]
};